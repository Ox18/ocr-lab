using System;
using System.Drawing;
using Model;

namespace Controller
{
    class ImagenController
    {
        private static String ImageFromText(Bitmap bmp)
        {
            return Imagen.ImageFromText(bmp);
        }
    }
}
